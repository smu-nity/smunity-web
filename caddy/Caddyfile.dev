:80 {
	encode zstd gzip
	root * /srv

	route {
		@api path /api/* /swagger-ui/* /v3/*
		handle @api {
			reverse_proxy {$BACKEND_HOST}
		}

		handle {
			try_files {path} {path}/ /index.html
			file_server
		}
	}
}
